=== isinstance check
a.is_a?(Dog)

=== isinstance with tuple of types
a.is_a?(Dog) || a.is_a?(Cat)

=== negated isinstance guard
if !v.is_a?(VTuple)

=== isinstance field path narrowing
st.expr.is_a?(Call)

=== isinstance on concrete type
info.is_a?(Info)
