=== set from dict items
for @{(do {

=== list from dict
my $ks = [sort keys %{$d}];

=== set from dict
my $s = do { my $__s = {}; $__s->{$_} = 1 for sort keys %{$d}; $__s };

=== list from dict keys method
my $ks = [sort keys %{$d}];

=== set from dict keys method
my $s = do { my $__s = {}; $__s->{$_} = 1 for sort keys %{$d}; $__s };

=== star unpack two lists
[ @{$a}, @{$b} ]

=== star unpack element then starred
[ $x, @{$rest} ]

=== star unpack starred then element
[ @{$xs}, $y ]

=== star unpack element starred element
[ $x, @{$middle}, $y ]

=== reversed list for loop
for my $x (reverse @{$xs}) {

=== reversed range for loop
for my $i (reverse 0 .. 4) {

=== zip two lists for loop
0 .. min(scalar(@{$a}), scalar(@{$b})) - 1) {

=== zip three lists for loop
0 .. min(scalar(@{$a}), scalar(@{$b}), scalar(@{$c})) - 1) {

=== zip bytes for loop
0 .. min(length($a), length($b)) - 1) {

=== string iteration for loop
for my $ch (split(//, $s)) {

=== list concat with plus
@{$a}, @{$b}

=== list repetition with annotation
[(0) x 5]
