=== isinstance check
(UNIVERSAL::isa($a, 'Dog'))

=== isinstance with tuple of types
(UNIVERSAL::isa($a, 'Dog')) || (UNIVERSAL::isa($a, 'Cat'))

=== isinstance with str
(!ref($x) && !looks_like_number($x))

=== isinstance with int
looks_like_number($x)

=== isinstance user class shadows builtin name
(UNIVERSAL::isa($x, 'List'))
