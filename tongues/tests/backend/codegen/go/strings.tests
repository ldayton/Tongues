=== string multiply via Repeat
s := strings.Repeat("ab", 3)

=== list multiply via Repeat
xs := make([]int, 5)

=== Contains on string
if strings.Contains(s, "world")

=== Split and Join
parts := strings.Split("a,b,c", ",")

=== string Find
idx := strings.Index("hello world", "world")

=== Replace on string
s := strings.ReplaceAll("aabbcc", "bb", "XX")

=== Upper and Lower
fmt.Println(strings.ToUpper("hello"))

=== StartsWith and EndsWith
fmt.Println(strconv.FormatBool(strings.HasPrefix(s, "hello")))

=== Trim functions
s := strings.Trim("  hello  ", " ")

=== Count and RFind on string
c := strings.Count("abcabc", "abc")

=== SplitN and SplitWhitespace
parts := strings.SplitN("a,b,c,d", ",", 2)

=== Concat strings
s := "hello" + " world"

=== string indexing and slicing
_ = s[1:4]

=== format string becomes f-string
return fmt.Sprintf("hello, %s", name)

=== Reverse on string
func reverseString(

=== format string with quotes in expression
return fmt.Sprintf("result: %s", strings.Join(items, ", "))

=== string classification functions
unicode.IsDigit(
