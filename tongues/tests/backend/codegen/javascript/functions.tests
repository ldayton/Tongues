=== function as value
return f(x);

=== function literal expression body
const result = apply((x) => x * 2, 10);

=== Main emits as lowercase main
function main(

=== function literal block body
apply((x) => {
    console.log(_pyStr(x));
}, 42);
