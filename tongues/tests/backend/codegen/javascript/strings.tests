=== string multiply via Repeat
const s = "ab".repeat(Math.max(0, 3));

=== list multiply via Repeat
const xs = Array(Math.max(0, 5)).fill(0);

=== Contains on string
if (s.includes("world"))

=== Split and Join
const parts = "a,b,c".split(",");

=== string Find
const idx = "hello world".indexOf("world");

=== Replace on string
const s = "aabbcc".replaceAll("bb", "XX");

=== Upper and Lower
console.log("hello".toUpperCase());

=== StartsWith and EndsWith
console.log(_pyStr(s.startsWith("hello")));

=== Trim functions
const s = "  hello  ".replace(/^[ ]+|[ ]+$/g, "");

=== Count and RFind on string
const c = "abcabc".split("abc").length - 1;

=== SplitN and SplitWhitespace
const words = "  hello   world  ".trim().split(/\s+/);

=== Concat strings
const s = "hello" + " world";

=== string indexing and slicing
const sub = s.slice(1, 4);

=== format string becomes f-string
return `hello, ${name}`;

=== Reverse on string
[..."hello"].reverse().join("");

=== format string with quotes in expression
return `result: ${items.join(", ")}`;

=== string classification functions
console.log(_pyStr(/^\d+$/.test("123")));
