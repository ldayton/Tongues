=== nil to interface variable
my $result = undef

=== interface common field accessor
return $e->{pos}

=== IsType narrows for field access
if ((UNIVERSAL::isa($it, 'Book')))

=== type switch binding shadows type name
if (UNIVERSAL::isa($operand, 'VInt'))

=== empty map in return statement
return {}

=== match case binding shadows type in body
if (UNIVERSAL::isa($v, 'VInt'))

=== map literal with interface value in return
return { "a" => Leaf->new(1) }

=== default string param fills zero value
say(greet())

=== optional string field deref
return $c->{name}

=== map get with string value type
return (exists $m->{$key} ? $m->{$key} : "")

=== map keys with string key type
return [sort keys %{$m}]

=== optional return forwarding no double wrap
return maybe_first($xs)

=== empty struct list return
return $list->()

=== list literal with interface type in return
return [Leaf->new(1)]
