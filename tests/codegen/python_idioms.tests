=== no skip_super_init marker
class MyError(Exception):
    def __init__(self, msg: str) -> None:
        super().__init__(msg)
        self.msg: str = msg
--- python
def NewMyError(msg: str) -> MyError:
    self = MyError()
    self.msg = msg
    return self
---

=== int conversion uses int() not helper
def parse_num(s: str) -> int:
    return int(s, 16)
---

=== str conversion uses str() not helper
def to_string(n: int) -> str:
    return str(n)
---

=== tuple unpacking from pop uses pop
@dataclass
class QuoteState:
    single: bool = False
    double: bool = False
    _stack: list[tuple[bool, bool]] = field(default_factory=list)

    def pop(self) -> None:
        if self._stack:
            self.single, self.double = self._stack.pop()
---
